# lf (list files) - minimal terminal file manager: https://github.com/gokcehan/lf
# Docs and config examples: https://github.com/gokcehan/lf/wiki

set hidden                # Show hidden files by default

# Open files with nvim using the 'e' key
map e $nvim $f              # Map 'e' to open current file with nvim



# Execute current file (must be executable)
map x $$f                 # Map x to execute current file in a subshell
map X !$f                 # Map X to execute current file in background
map ` !true               # Map ` to show result of previous command (example)
