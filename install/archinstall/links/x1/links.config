# Xmonad configuration for x1

# Dotfiles directory (where stow packages are located)
DOTFILES_DIR="$BASE_DIR/dotfiles"

# Space-separated list of packages to stow
# Each package should be a directory in DOTFILES_DIR with files/dirs structured
# to mirror the target location relative to $HOME
packages="vim bash nvim x dunst systemd lf vscode brave gdb"

# Example package structures:
# - zsh/.zshrc -> ~/.zshrc
# - xmobar/.config/xmobar/xmobarrc -> ~/.config/xmobar/xmobarrc
# - nvim/.config/nvim/ -> ~/.config/nvim/
